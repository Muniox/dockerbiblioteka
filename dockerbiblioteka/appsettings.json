{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
  // Konfiguracja aplikacji nie jest tutaj — całość idzie przez zmienne środowiskowe:
  // .env -> compose.yaml, a w Portainerze przez "Environment variables" stacka.
  //   KeycloakAuth__{Authority,ClientId,ClientSecret,RedisConnectionString,SessionKeyNamespace}
  //   ReverseProxy__KnownNetwork  (puste = brak proxy)
}
